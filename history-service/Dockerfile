FROM python:3.12-slim
ENV PYTHONUNBUFFERED=1 PIP_NO_CACHE_DIR=1
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["sh", "-c", "gunicorn -b 0.0.0.0:${PORT:-8080} main:app --workers 1 --threads 8 --timeout 120"]
